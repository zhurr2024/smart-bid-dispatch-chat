# 智能标讯分发系统 · 数据来源与取数逻辑说明

> 适用版本：v1.0 | 更新日期：2026-04-30

---

## 一、数据来源

本项目为 **Demo / Mock 模式**，前端通过 **MSW（Mock Service Worker）** 拦截所有 API 请求，数据完全来自前端内存，无需真实后端。

```
浏览器请求 → MSW 拦截 → 从内存 mockBids[] 取数 → 返回 JSON
```

### Mock 数据集位置

```
frontend/src/mocks/data/bids.ts
```

- **共 20 条标讯**（`bid-001` ～ `bid-020`）
- 数据在应用启动时加载到内存，支持增删改操作（内存级别，**刷新后重置**）

---

## 二、标讯数据结构

每条标讯包含以下关键字段：

| 字段 | 说明 | 示例值 |
|------|------|--------|
| `bidNo` | 标讯编号 | `ISG-2024-0312-001` |
| `bidType` | 标讯类型 | `ISG`（16条）/ `SSG`（4条）|
| `tenderType` | 招标类型 | `INTENT`（意向招标）/ `FORMAL`（实时招标）|
| `priority` | 优先级 | `HIGH`（6条）/ `MEDIUM`（8条）/ `LOW`（6条）|
| `status` | 跟进状态 | 8 种状态（见第三节）|
| `region` | 大区 | 华南 / 华东 / 华北 / 西南 / 华中 |
| `budget` | 预算（万元）| 28 ～ 4500 |
| `projectName` | 项目名称 | 广州市第一人民医院智慧园区基础设施采购项目 |
| `purchaserName` | 采购方 | 广州市第一人民医院 |
| `summary` | 标讯摘要 | 结构化描述（见第六节）|
| `publishedAt` | 发布时间 | 相对当前时间动态生成 |
| `deadlineAt` | 截止时间 | 相对当前时间动态生成 |
| `keywords` | 关键词标签 | `['医疗IT', '服务器', '存储', '网络']` |
| `source` | 数据来源渠道 | 政府采购网 / 中国招标网 等 |

---

## 三、状态流转（8 个状态）

```
PENDING（待分配）
  └─→ ASSIGNED（已分配）       ← 销管 / HQ 手工分配或系统自动分派
        └─→ RECEIVED（已接收） ← AR 确认接收
              └─→ IN_PROGRESS（跟进中）  ← AR 开始跟进
                    └─→ OPPORTUNITY（商机上报）← AR 上报商机
                          ├─→ WON（赢单）
                          ├─→ LOST（输单）
                          └─→ ABANDONED（已放弃）
```

> **SSG 类型标讯**：AR 只需标记已读，无需进行商机上报及后续闭环。

---

## 四、统计指标取数逻辑

数据来源文件：`frontend/src/mocks/handlers/reportHandlers.ts`

所有指标均**从 `mockBids[]` 内存数组实时聚合计算**：

| 指标 | 取数逻辑 | 当前 Mock 值 |
|------|---------|------------|
| **标讯总量** | `mockBids.length` | **20 条** |
| **高优先级** | `filter(priority === 'HIGH')` | **6 条** |
| **意向标讯** | `filter(tenderType === 'INTENT')` | **10 条** |
| **实时标讯** | `filter(tenderType === 'FORMAL')` | **10 条** |
| **待分配** | `filter(status === 'PENDING')` | **5 条** |
| **赢单数** | `filter(status === 'WON')` | **1 条** |
| **转化率** | `赢单数 ÷ 已结案数（WON+LOST+ABANDONED）` | **约 33%** |
| **平均响应时长** | 硬编码固定值（Demo 占位）| **18.5 小时** |

---

## 五、大区分布（实时统计）

| 大区 | 标讯总数 | 赢单数 |
|------|---------|-------|
| 华南 | 12 | 1 |
| 华东 | 3 | 0 |
| 华北 | 2 | 0 |
| 西南 | 2 | 0 |
| 华中 | 1 | 0 |

> 取数逻辑：按 `region` 字段 `group by`，分别统计 `count` 和 `status === 'WON'` 的数量。

---

## 六、WinLoss 漏斗（从状态实时汇总）

| 漏斗层级 | 筛选条件 | 当前值 |
|---------|---------|-------|
| 标讯总量 | 全量 | 20 |
| 已分配 | `status !== 'PENDING'` | 15 |
| 跟进中 | `status IN (IN_PROGRESS, OPPORTUNITY, WON, LOST)` | 7 |
| 商机上报 | `status IN (OPPORTUNITY, WON, LOST)` | 3 |
| 赢单 | `status === 'WON'` | 1 |

---

## 七、标讯摘要（Summary）生成规则

每条标讯的 `summary` 字段为**人工编写的结构化描述**，统一格式：

```
[项目背景] + [采购内容（设备/软件类型）] + [建设目标]
```

**示例：**

> *"广东省政务云平台二期扩容，采购高性能计算节点、超融合基础设施及安全防护设备，构建统一政务云底座。"*

> *"华山医院 HIS 系统升级改造，采购数据库服务器、应用服务器及存储设备，保障医疗业务稳定运行。"*

---

## 八、优先级自动评分算法

文件：`frontend/src/skills/bid-analysis/priorityEngine.ts`

评分规则（满分 100 分）：

| 条件 | 加分 |
|------|-----|
| 招标类型 = 意向招标（INTENT） | +30 |
| 招标类型 = 实时招标（FORMAL） | +10 |
| 预算 ≥ 1000 万 | +40 |
| 预算 500 ～ 999 万 | +30 |
| 预算 100 ～ 499 万 | +15 |
| 预算 < 100 万 | +0 |
| 截止日期 ≤ 7 天 | +30 |
| 截止日期 ≤ 30 天 | +15 |
| 截止日期 > 30 天 | +5 |
| 信息不完整（缺名称/采购方/摘要/预算） | -10 |

**等级划分：**

| 分数区间 | 优先级 | 颜色标识 |
|---------|--------|---------|
| ≥ 70 分 | HIGH（紧急）| 🔴 红色 |
| 40 ～ 69 分 | MEDIUM（中优）| 🟡 黄色 |
| < 40 分 | LOW（低优）| ⚪ 灰色 |

---

## 九、后端对接说明

当前 Mock API 端点与真实后端**接口契约一致**，上线时只需：

1. 移除 `main.tsx` 中的 MSW 启动代码
2. 在 `frontend/src/services/api.ts` 中配置真实后端 `BASE_URL`
3. 确保后端返回数据结构与 `src/types/index.ts` 中的类型定义保持一致

| Mock API 路径 | 说明 |
|--------------|------|
| `GET /api/v1/bids` | 标讯列表（支持筛选/分页）|
| `GET /api/v1/bids/:id` | 单条标讯详情 |
| `PUT /api/v1/bids/:id/assign` | 分配标讯给 AR |
| `PUT /api/v1/bids/:id/status` | 更新跟进状态 |
| `GET /api/v1/reports/overview` | 统计总览指标 |
| `GET /api/v1/reports/trend` | 月度趋势数据 |
| `GET /api/v1/reports/region-dist` | 大区分布数据 |
| `GET /api/v1/reports/winloss-funnel` | WinLoss 漏斗数据 |

---

*文档自动生成 · Smart Bid Dispatch Chat v1.0*
